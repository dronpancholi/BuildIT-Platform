"use client";

/**
 * Operator Command Center — the page an SEO manager lands on
 * when they want to know "is the platform healthy?" and
 * "what needs my attention?".
 */

import { OperatorCommandCenter } from "@/components/operator/operator-command-center";

export default function CommandCenterPage() {
  return <OperatorCommandCenter />;
}
